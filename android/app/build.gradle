plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.safe_ping_1"
    compileSdk = 35  // Latest stable SDK version
    ndkVersion = "26.1.10909125" // Ensure this is installed

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "com.example.safe_ping_1"
        minSdk = 21  // Ensuring compatibility with older devices
        targetSdk = 35  // Updated to match compileSdk
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            // Removed debug signing for better security
            minifyEnabled false
            shrinkResources false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

flutter {
    source = "../.."
}
